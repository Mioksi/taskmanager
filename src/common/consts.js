const TASK_COUNT = 3;

const Place = {
  BEFOREEND: `beforeend`,
  AFTEREND: `afterend`
};

const FILTER_NAMES = [
  `all`, `overdue`, `today`, `favorites`, `repeating`, `archive`
];

export {TASK_COUNT, Place, FILTER_NAMES};
